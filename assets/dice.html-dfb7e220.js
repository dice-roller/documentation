import{_ as s,r as n,o as i,c,b as d,e as a}from"./app-d7a80b43.js";const o={},t=a(`<h1 id="dice" tabindex="-1"><a class="header-anchor" href="#dice" aria-hidden="true">#</a> Dice</h1><p>The simplest form of notation is just a basic dice roll.</p><div class="custom-container warning"><p class="custom-container-title">Die quantity</p><p>A single die has a minimum quantity of <code>1</code>, and a maximum quantity of <code>999</code>.</p><p>✅ These are valid: <code>d8</code>, <code>1d10</code>, <code>999d6</code>, <code>20d4 + 999d10</code>.</p><p>❌ These are not: <code>0d10</code>, <code>1000d6</code>, <code>-1d20</code>.</p></div><h2 id="standard" tabindex="-1"><a class="header-anchor" href="#standard" aria-hidden="true">#</a> Standard</h2><p><strong>Notation:</strong> <code>d{n}</code></p><p>A standard die has a positive numerical number of sides, like typical 6 sided dice, or a d20.</p><p>You can roll dice with almost any number of sides.</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>d6    <span class="token comment">// roll a single 6 sided dice</span>
4d10  <span class="token comment">// roll a 10 sided dice 4 times and add the results together</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div>`,8),r=a(`<h2 id="percentile-dice" tabindex="-1"><a class="header-anchor" href="#percentile-dice" aria-hidden="true">#</a> Percentile dice</h2><p><strong>Notation:</strong> <code>d%</code></p><p>Percentile dice roll a whole number between <code>1-100</code>, and are specified with the format <code>d%</code>. This is a shorthand for a standard die with 100 sides, <code>d100</code></p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>4d<span class="token operator">%</span>  <span class="token comment">// roll a percentile die 4 times and add the results together</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Is equivalent to:</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>4d100  <span class="token comment">// roll a 100 sided die 4 times and add the results together</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,6),l=a('<h2 id="fudge-fate-dice" tabindex="-1"><a class="header-anchor" href="#fudge-fate-dice" aria-hidden="true">#</a> Fudge / Fate dice</h2><p><strong>Notation:</strong> <code>dF</code> / <code>dF.2</code> / <code>dF.1</code></p><p>Fudge dice are 6 sided dice marked with minus (<code>-</code>), plus (<code>+</code>), and blank sides, which correlate to <code>-1</code>, <code>+1</code>, and <code>0</code> respectively.</p><p>The default is one third of each, known as <code>dF.2</code>, or just <code>dF</code>. A variant is <code>dF.1</code> which has 4 blanks, 1 plus, and 1 minus.</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>dF    <span class="token comment">// roll a standard Fudge die. Equivalent to `dF.2`</span>\ndF<span class="token punctuation">.</span><span class="token number">2</span>  <span class="token comment">// roll a standard Fudge dice. Equivalent to `dF`</span>\ndF<span class="token punctuation">.</span><span class="token number">1</span>  <span class="token comment">// roll the variant Fudge dice</span>\n4dF   <span class="token comment">// Roll a standard Fudge die 4 times and add the results together</span>\n</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>',5);function p(u,m){const e=n("DiceRoller");return i(),c("div",null,[t,d(e,{notation:"4d10"}),r,d(e,{notation:"4d%"}),l,d(e,{notation:"4dF"})])}const v=s(o,[["render",p],["__file","dice.html.vue"]]);export{v as default};
